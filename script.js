const fs = require('fs');
const readline = require('readline');

let 

let dataSet = [];
let randomNumber = (dataSet.length>=1)?Math.floor(Math.random()*dataSet.length):"Nothing found in list";

console.log(randomNumber);
